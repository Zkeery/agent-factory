# 第 6 阶段证据包｜做实代码生成：产出能本地跑的最小 AI 应用

## 证据文件清单

| 文件 | 内容 |
| --- | --- |
| [1-生成的app.py.txt](1-生成的app.py.txt) | 真实模型生成的完整 FastAPI 应用（记账小工具） |
| [2-requirements.txt](2-requirements.txt) | 生成的依赖清单 |
| [3-README.md](3-README.md) | 生成的启动说明 |

## 本阶段做了什么

把工厂「构建」环节从"骨架占位"升级为**能本地跑的最小可运行 AI 应用**：

- 代码生成 prompt 重写：生成完整 FastAPI 应用（app.py + requirements.txt + README），三块分隔；
- 测试工序升级：语法检查 + 依赖检查 + import 检查（不再是"非空即过"）；
- 部署脚本适配：pip install + uvicorn 启动。

## 真实冒烟结果（核心证据）

想法「做一个帮我记账的小工具」→ 真实 DeepSeek 生成完整应用：

1. **生成的产品**：完整 FastAPI 应用，含 `POST /generate` 接口、DeepSeek 调用、记账系统提示词、错误处理（无 Key 返回明确错误）。
2. **能跑**：uvicorn 启动成功；无 Key 时调接口返回 `{"detail":"缺少 DEEPSEEK_API_KEY"}` + HTTP 500。
3. **真实调用**：配 Key 后调 `/generate`，输入"昨天吃饭花了50块，今天买书花了80"，返回结构化记账条目 + 分类建议，HTTP 200。

## 验证结果

- ✅ 后端测试 53/53（新增：三块生成、依赖检查、fastapi 缺失拦截）
- ✅ 真实模型生成完整应用 + 能跑 + 真实调用成功

## 阶段闸门是否表

| # | 检查项 | 是 / 否 |
| --- | --- | --- |
| 1 | 本阶段范围内功能已实现，没有提前做后续阶段 | 是 |
| 2 | mock 自动化测试已运行且通过 | 是（53/53） |
| 3 | 真实模型冒烟已运行 | 是（DeepSeek 生成完整应用 + 真实调用成功） |
| 4 | 类型检查 / Lint / 生产构建通过（有前端时） | N/A（纯后端改动） |
| 5 | 密钥未进入代码、仓库、前端产物、打包文件 | 是（生成的产品读环境变量，不硬编码 Key） |
| 6 | 数据在服务重启后可恢复，已实际验证 | 是 |
| 7 | 桌面与移动端宽度已检查（有界面时） | N/A（无界面改动） |
| 8 | README 已更新启动与验证方法 | 是 |
| 9 | docs/项目状态.md 已更新 | 是 |
| 10 | 证据包已交付 | 是 |

## 待验说明

生成的产品无前端（API 优先），完整前端后续阶段再做。生成代码质量依赖模型，以三维打分（代码维度）衡量，结构校验保证"能跑"不保证"业务完美"。
