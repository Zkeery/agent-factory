{
  "tool_list": [
    {
      "name": "call_model_api",
      "input": "阶段任务生成请求、模型参数、产品经理自带 API Key 的安全引用",
      "output": "模型生成结果（拆需求、定架构、代码、测试结果等）",
      "side_effect": "调用外部模型接口，产生按量费用",
      "permission": "allow",
      "layer": "待确认"
    },
    {
      "name": "persist_project_artifact",
      "input": "制品类型（PRD、项目状态、决策台账、证据包）、内容、版本",
      "output": "持久化记录 ID",
      "side_effect": "写入长期保留的项目存储",
      "permission": "allow",
      "layer": "待确认"
    },
    {
      "name": "persist_runtime_state",
      "input": "任务 ID、长任务状态、人工确认点状态、业务数据",
      "output": "持久化保存确认",
      "side_effect": "写入可恢复的运行时状态存储",
      "permission": "allow",
      "layer": "待确认"
    },
    {
      "name": "deploy_and_isolate_account",
      "input": "部署包/配置、国内目标环境、租户/账号隔离信息",
      "output": "部署状态、访问地址、隔离校验结果",
      "side_effect": "执行上线部署与账号隔离，属于对外动作",
      "permission": "ask",
      "layer": "待确认"
    },
    {
      "name": "request_human_confirmation",
      "input": "展示内容（进度、问题、证据包引用）、所需回复/评分类型",
      "output": "产品经理回复、评分或确认",
      "side_effect": "产生通知并阻塞等待人工输入",
      "permission": "ask",
      "layer": "待确认"
    },
    {
      "name": "verify_phone_login",
      "input": "手机号、验证码",
      "output": "身份验证结果",
      "side_effect": "调用短信验证码验证服务，产生身份认证状态",
      "permission": "allow",
      "layer": "待确认"
    },
    {
      "name": "check_tenant_isolation",
      "input": "租户 ID、请求资源 ID、用户身份",
      "output": "是否允许访问",
      "side_effect": "无业务副作用，仅执行后端权限判断",
      "permission": "deny",
      "layer": "待确认"
    }
  ],
  "system_prompt": "你是 Agent造物坊 的固定流程执行 Agent。身份：非技术产品经理的 AI 产品制造执行器，不自行决定高风险动作。规则：严格按阶段推进（拆需求→定架构→写代码→测试→上线）；每阶段必须通过闸门并产出证据包，不靠删测试、降标准、mock冒充通过；高风险、不可逆、涉及费用或对外的动作必须由确定性代码和人工确认；密钥不进仓库、前端、对话、日志、截图或提交记录；数据与部署国内不出境。工具：call_model_api 调用用户自带 Key 模型；persist_project_artifact、persist_runtime_state 保存项目与运行状态；deploy_and_isolate_account 执行部署前需人工确认；request_human_confirmation 获取人工确认与评分；verify_phone_login 完成身份验证；check_tenant_isolation 由后端强制执行。知识目录：用户想法/PRD草稿、固定工序、闸门标准与证据包要求、安全红线与密钥纪律、国内部署约束、长期保留的项目资产。",
  "context_memory_strategy": "会话内按阶段隔离上下文，仅载入当前阶段必要的输入、产物和闸门标准，上一阶段完整产物外置到持久化存储并通过引用访问；不在上下文中保留密钥、验证码或敏感凭据。跨会话持久化：PRD、项目状态、决策台账、证据包、业务数据、长任务状态、人工确认点必须持久化，进程重启后可恢复。是否引入上下文压缩或摘要策略，需待上下文压缩组件确认后确定。",
  "security_and_degradation": [
    "call_model_api 请求超时或失败 → 进入重试或暂停，保留失败上下文并写入决策台账，不得跳过阶段闸门，人工确认后继续。",
    "模型幻觉生成虚构事实或未达到闸门标准 → 闸门检查对照输入与证据包拦截，拒绝通过并记录，不降低标准，要求重新生成。",
    "长任务或部署超时 → 任务系统持久化当前状态，恢复后从检查点继续；部署超时需人工确认继续或回滚。",
    "模型越权尝试调用高风险、对外或涉及费用动作 → 权限系统默认拒绝，deploy_and_isolate_account 必须 ask，check_tenant_isolation 为 deny，记录越权审计并通知产品经理。",
    "密钥泄漏或回显风险 → 持久化、日志与轨迹记录过滤并禁止密钥字段，审计日志检查；发现即阻断并脱敏。",
    "跨租户访问尝试 → check_tenant_isolation 后端强制执行，返回 deny，记录告警，阻止跨租户数据访问。"
  ],
  "observability": {
    "logs": [
      "记录每次阶段推进的输入、闸门结果、证据包引用和人工确认结果。",
      "记录每次工具调用的名称、参数元数据、耗时和结果状态，不记录密钥、验证码明文。",
      "记录部署与账号隔离的国内目标环境、确认状态和结果。",
      "记录越权拒绝与敏感动作拦截审计日志。"
    ],
    "metrics": [
      "阶段首次通过率与最终通过率。",
      "每阶段耗时与项目总耗时。",
      "工具调用失败率与重试次数。",
      "人工确认等待时长与确认次数。",
      "单款模型费用统计。"
    ],
    "trajectory": "轨迹按项目与阶段记录每一步输入、工具调用、产物、闸门判定和人工确认，写入决策台账/证据包持久化存储，不记录密钥、验证码明文。"
  },
  "tech_stack_and_deployment": "第一版部署形态与实现语言：待确认（输入未明确）。模型来源：产品经理自带 API Key，工厂不垫钱、不代管统一模型、不做利润分成。部署与数据必须国内不出境。业务数据、长任务状态和人工确认点必须持久化，进程重启后可恢复。对外账号、付费、多租户后置。",
  "open_questions": [
    "7层架构图的层定义未在输入中给出，无法将工具归属到①-⑦，需补充层定义。",
    "人工交互渠道具体是 UI 还是消息待确认。",
    "第一版部署形态（单进程/服务/团队）与实现语言需确认。",
    "上下文压缩、子 Agent、技能加载、定时调度、Agent 团队、MCP 插件是否纳入第一版仍为待确认。",
    "单款成本上限、“一天一款”时间口径、时延与并发指标需实测后确定。"
  ]
}